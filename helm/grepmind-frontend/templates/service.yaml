apiVersion: v1
kind: Service
metadata:
  name: {{ include "grepmind-frontend.fullname" . }}
  labels:
    {{- include "grepmind-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "grepmind-frontend.selectorLabels" . | nindent 4 }}